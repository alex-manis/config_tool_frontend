# Как загрузить Frontend в отдельный репозиторий

Frontend теперь **полностью независим** и готов к загрузке в отдельный репозиторий.

## ✅ Что было сделано

1. ✅ Типы скопированы в `frontend/types/` (не зависят от `shared/`)
2. ✅ Создан отдельный `package.json` для frontend
3. ✅ Создан `tsconfig.json` для frontend
4. ✅ Обновлены импорты во всех файлах
5. ✅ Frontend компилируется независимо

## 📁 Структура Frontend

```
frontend/
├── src/                 # Исходный TypeScript код
│   ├── api/            # API клиент
│   ├── components/     # Компоненты UI
│   ├── state/          # Управление состоянием
│   ├── utils/          # Утилиты
│   └── types/          # TypeScript типы (независимые)
├── public/             # Статические файлы
│   ├── assets/         # Изображения
│   ├── client/         # Скомпилированный JS (генерируется)
│   ├── index.html      # Главная страница
│   └── styles.css      # Стили
├── package.json        # Зависимости frontend
├── tsconfig.json       # Конфигурация TypeScript
├── .gitignore         # Игнорируемые файлы
├── README.md          # Документация
└── DEPLOY.md          # Эта инструкция
```

## 🚀 Шаги для загрузки в новый репозиторий

### Вариант 1: Создать новый репозиторий из папки frontend

```bash
# 1. Перейти в папку frontend
cd frontend

# 2. Инициализировать git репозиторий
git init

# 3. Добавить все файлы
git add .

# 4. Создать первый коммит
git commit -m "Initial commit: Frontend application"

# 5. Добавить remote репозиторий (замените URL на свой)
git remote add origin https://github.com/yourusername/ddive-frontend.git

# 6. Загрузить код
git push -u origin main
```

### Вариант 2: Использовать существующий репозиторий

```bash
# 1. Клонировать ваш репозиторий
git clone https://github.com/yourusername/ddive-frontend.git
cd ddive-frontend

# 2. Скопировать все файлы из frontend/ в корень репозитория
# Windows PowerShell:
Copy-Item -Path "..\ddive_config_tool\frontend\*" -Destination "." -Recurse -Force

# 3. Добавить и закоммитить
git add .
git commit -m "Add frontend code"
git push
```

## 📦 После загрузки

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Проверьте, что все работает:**
   ```bash
   npm run build
   npm run serve
   ```

3. **Настройте API URL** (если нужно):
   - Отредактируйте `src/utils/constants.ts`
   - Или установите `window.API_BASE_URL` в `public/index.html`

## ⚙️ Команды

```bash
npm run dev      # Собрать и запустить dev сервер
npm run build    # Сборка TypeScript
npm run serve    # Запустить HTTP сервер
npm start        # Собрать и запустить (production)
```

## 🔄 Синхронизация типов

Если типы в основном проекте (`shared/types/interfaces.ts`) изменятся, нужно будет вручную обновить `frontend/types/interfaces.ts`.

**Альтернативы:**
- Git submodule (для продвинутых)
- npm пакет для общих типов
- Простое копирование при необходимости (рекомендуется для простоты)

## 🌐 Развертывание

После сборки (`npm run build`) папку `public/` можно развернуть на:
- **GitHub Pages**: `npm run build && gh-pages -d public`
- **Netlify**: подключить репозиторий, указать build команду `npm run build` и publish directory `public`
- **Vercel**: аналогично Netlify
- **Любой веб-сервер**: просто скопировать `public/` на сервер

## ✅ Проверка независимости

Frontend полностью независим:
- ✅ Не зависит от `shared/` папки
- ✅ Не зависит от `backend/`
- ✅ Имеет свой `package.json`
- ✅ Имеет свой `tsconfig.json`
- ✅ Компилируется самостоятельно

## 🔗 Настройка API

По умолчанию фронтенд обращается к `http://localhost:3001/api`.

Для production измените `BASE_URL` в `src/utils/constants.ts` или используйте переменную окружения через сборку.


